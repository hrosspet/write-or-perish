# Development Dockerfile for Write or Perish Frontend
# Uses React dev server with hot module replacement

FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci --legacy-peer-deps

# Copy source code (will be overridden by volume mount in dev)
COPY . .

# Environment variables for hot reloading in Docker
ENV CHOKIDAR_USEPOLLING=true
ENV WATCHPACK_POLLING=true
ENV CI=false

# Expose port for React dev server
EXPOSE 3000

# Default command
CMD ["npm", "start"]
