**/__pycache__
!.gitignore
!.gitattributes
.claude/

__pycache__/
*.py[cod]
*.egg-info/
*.egg
env/
venv/
ENV/
.venv/
**.env*
!.env.staging.example
.flaskenv

# Byte-compiled / optimized / DLL files
*.pyc
*.pyo
*.pyd
*.swp

# Logs and local settings
*.log
instance/
.webassets-cache

# Flask/Migrate/Node autogenerated files
# Track migration version files but ignore other migration artifacts
migrations/__pycache__/
migrations/versions/__pycache__/
migrations/README
migrations/alembic.ini
migrations/env.py
migrations/script.py.mako
node_modules/

# Other OS-specific files
.DS_Store
Thumbs.db


*.zip
prompt.txt
data/

# GCP credentials
kms-credentials.json
*-credentials.json

# Deployment files
deployment.log
gunicorn.pid
frontend-build.tar.gz
frontend/build-staging/
logs/

# Experiment-specific prompts (vary per run)
experiments/prompt_rct/eval_prompts/
# Staging-eval fixture (real content; never commit) — #155/#197
staging_fixture*.json

# Cheap-model intentions eval (experiments/cheap-model-intentions):
# generated artifacts + rendered prompt contain a named person's corpus
# and inferred profile — public repo, keep them local.
intentions_*.txt
experiments/cheap-model-intentions/*.json
experiments/cheap-model-intentions/prompt.txt

# Regenerable in ~4 min from the snapshot; lists 734 real accounts ranked for
# profiling, so keep it out of the public repo.
ca_token_census.tsv
experiments/cheap-model-intentions/chunk_batch_state.json
